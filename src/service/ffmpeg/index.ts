import { FfmpegCommand } from "fluent-ffmpeg"
export class FFMPEGWrapper {
	public static async convertToTargetFormat(): Promise<unknown> {
		return await new Promise((resolve, reject) => {
			resolve()
		})
	}
}